// Copy this file to secrets.h and fill in your real WiFi credentials.
// secrets.h is gitignored — it never gets committed or pushed.
#define WIFI_SSID "your-wifi-name"
#define WIFI_PASS "your-wifi-password"
